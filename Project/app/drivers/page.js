import DriversPage from './DriversPage';

export const metadata = {
  title: 'Fleet & Driver Management — FreightProxy',
  description: 'Manage active carrier fleet, driver directories, and vehicle assignments.',
};

export default function Page() {
  return <DriversPage />;
}
