import DispatcherPage from './DispatcherPage';

export const metadata = {
  title: 'Dispatcher Staging Hub — FreightProxy',
  description: 'Date-filtered shipment staging, truck allocations, and fleet driver coordinator.',
};

export default function Page() {
  return <DispatcherPage />;
}
